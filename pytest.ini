[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    fota: long-running FOTA reliability experiment; excluded from per-commit CI
    demo_failure: Demo failure test for Allure screenshot sample — excluded from formal tests
